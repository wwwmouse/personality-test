{
  "name": "personality-test",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "npm --prefix server install && npm --prefix frontend install && npm --prefix frontend run build"
  }
}
