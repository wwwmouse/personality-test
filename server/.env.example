# 使用说明：把本文件复制一份，改名为 .env，把下面的假 key 换成你自己的
# .env 已被 .gitignore 挡住，永远不会被 git 上传
DEEPSEEK_API_KEY=sk-在这里填你的key

# 统计页口令：/stats 页凭它查看账本。本地 .env 和 Vercel 后台环境变量各配一份，
# 自设一个记得住的就行，不要用"123456"这种一猜就中的
STATS_KEY=在这里设你的口令
